logging.level.root=INFO
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate=ERROR
# DataSource settings: set here your own configurations for the database
# connection. In this example we have "linkusDB" as database name and
# "root" as username and password.
datasource.primary.jdbc.url=jdbc:mysql://localhost/linkusDB
datasource.primary.jdbc.user=root
datasource.primary.jdbc.pass=root
datasource.primary.jdbc.driverClassName=com.mysql.jdbc.Driver
spring.thymeleaf.mode=LEGACYHTML5
# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

#spring.mail.properties.mail.smtp.starttls.enable=true
#spring.mail.properties.mail.smtp.starttls.required=true
#spring.mail.properties.mail.smtp.auth = true;
#spring.mail.host=smtp-mail.outlook.com
#spring.mail.port = 587

facebook.clientId=355532101482697
facebook.clientSecret=9e546add05e66c3be13ccfbbea273512



